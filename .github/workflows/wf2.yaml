
description: Triggers GitHub Wf from multiple event triggers
name: Building first workfloe blocks

on:
  push:
  pull_request:
  schedule:
    - cron: '5 * * * *'
  workflow_dispatch:

jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
            - name: step1
              run: echo "Run step1!"

    job2:
        runs-on: ubuntu-latest
        steps:
            - name: failed step
              run: |
                echo "I failed"
                exit 0              # terminate process execution
            - name: failed
              run: echo "Goodbyeee"