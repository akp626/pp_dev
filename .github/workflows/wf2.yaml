
description: Triggers GitHub Wf from multiple event triggers
name: Building first workfloe blocks

on:
  push:
  pull_request:
#   schedule:
#     - cron: '5 * * * *'
#   workflow_dispatch:

jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
            - name: step1
              run: echo "Trigger from ${{github.event_name}}"
