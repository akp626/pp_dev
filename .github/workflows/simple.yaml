name: Building first workfloe blocks

on: push

jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
            - name: step1
              run: echo "Run step1!"

    job2:
        runs-on: ubuntu-latest
        steps:
            - name: failed step
              run: |
                echo "I failed"
                exit 1              # terminate process execution
            - name: failed
              run: echo "Goodbye"