name: Building first workfloe blocks

on: push

jobs:
    flow1:
        runs-on: ubuntu-latest
        steps:
            - name: step1
              run: echo "Run step1!"

    flow2:
        runs-on: ubuntu-latest
        steps:
            - name: failed step
              run: |
                echo "I failed"
                exit 0
            - name: failed
              run: echo "Goodbye"